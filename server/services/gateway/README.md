Gateway

Purpose: Single public entrypoint for routing, rate limiting, TLS termination, and central auth checks.

This folder will host the configuration for the API Gateway (Express-based or a lightweight proxy) that forwards to internal services.